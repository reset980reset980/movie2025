# 🎓 초등학교 졸업영상 자동 제작 프로그램 - 작업지시서 v2.0 (실용 개선판)

## 📌 프로젝트 개요

**목적**: Google Colab에서 실행 가능한 졸업영상 자동 제작 노트북 개발
**사용 환경**: Google Colab (무료 버전)
**최종 산출물**: 5분 이내 Full HD(1920x1080) MP4 영상
**대상**: 초등학교 졸업식 강당 상영용

**⚡ v2.0 개선 사항**:
- ✅ 한글 폰트 자동 설치 로직 추가
- ✅ 완전한 오류 처리 시스템 구현
- ✅ Google Drive 자동 저장 기능
- ✅ AI 이미지 생성 선택적 사용 (비용 절감 옵션)
- ✅ 진행률 실시간 표시 (tqdm)
- ✅ 샘플 데이터 구조 명시
- ✅ JSON 파싱 및 엑셀 읽기 로직 완성
- ✅ Colab 세션 타임아웃 대비 자동 체크포인트

---

## 🎯 핵심 요구사항

### 입력 데이터 구조

#### 1. 사진 폴더 (`photos/`)
- 수백 장의 JPG/PNG 이미지
- 파일명 예시: `운동회_001.jpg`, `소풍_2024.jpg`

#### 2. 영상 클립 폴더 (`videos/`)
- 10개 내외의 MP4/MOV 파일
- 파일명 예시: `체육대회.mp4`, `학예회.mp4`

#### 3. 증명사진 폴더 (`id_photos/`)
- 학생별 증명사진 (파일명 = 학생 이름)
- 예시: `홍길동.jpg`, `김영희.jpg`

#### 4. 학생 정보 엑셀 (`학생정보.xlsx`)

| 이름 | 어릴때사진경로 | 장래희망 |
|------|----------------|----------|
| 홍길동 | baby_photos/홍길동.jpg | 의사 |
| 김영희 | baby_photos/김영희.jpg | 선생님 |

#### 5. 학교 로고 (`logo.png`)
- 투명 배경 PNG 권장
- 최소 해상도: 1000×1000

#### 6. 배경음악 (`bgm.mp3`)
- Suno AI로 제작한 MP3
- 길이: 5-6분 권장

---

## 🛠️ 기술 스택

### 필수 라이브러리
```python
moviepy==1.0.3
google-generativeai==0.3.2
pillow==10.1.0
pandas==2.1.4
numpy==1.24.3
opencv-python==4.8.1
tqdm==4.66.1
```

### AI 서비스
- **Gemini 2.0 Flash**: 시나리오 생성, 사진 분석 (무료)
- **Flux API** (선택): 미래 이미지 생성 (유료, 비활성화 가능)

---

## 📂 Colab 노트북 구조 (총 14개 셀)

### 📦 셀 1: 완전한 환경 설정

```python
"""
🚀 초등학교 졸업영상 자동 제작 프로그램 v2.0
- 한글 폰트 자동 설치
- 모든 필수 라이브러리 설치
- 진행률 표시 시스템
"""

print("📦 환경 설정 시작...")

# 1. 시스템 패키지 설치
import subprocess
import sys

def install_system_packages():
    """한글 폰트 및 시스템 도구 설치"""
    try:
        print("🔤 한글 폰트 설치 중...")
        subprocess.run(['apt-get', 'update'], check=True, capture_output=True)
        subprocess.run(['apt-get', 'install', '-y', 'fonts-nanum', 'fonts-nanum-extra'],
                      check=True, capture_output=True)
        subprocess.run(['fc-cache', '-fv'], check=True, capture_output=True)
        print("✅ 한글 폰트 설치 완료")
    except Exception as e:
        print(f"⚠️ 폰트 설치 실패 (계속 진행): {e}")

install_system_packages()

# 2. Python 라이브러리 설치
print("📚 Python 라이브러리 설치 중...")
packages = [
    'moviepy==1.0.3',
    'google-generativeai',
    'pillow',
    'pandas',
    'openpyxl',  # 엑셀 읽기용
    'tqdm',
    'requests'
]

for pkg in packages:
    subprocess.run([sys.executable, '-m', 'pip', 'install', '-q', pkg], check=True)

print("✅ 모든 라이브러리 설치 완료")

# 3. UTF-8 인코딩 설정
import locale
locale.getpreferredencoding = lambda: "UTF-8"

# 4. 경고 메시지 숨기기
import warnings
warnings.filterwarnings('ignore')

print("\n🎉 환경 설정 완료! 다음 셀을 실행하세요.\n")
```

---

### 💾 셀 2: Google Drive 마운트 및 작업 폴더 설정

```python
"""
Google Drive 연결 및 작업 폴더 생성
- 세션 종료 시에도 데이터 보존
- 자동 체크포인트 저장
"""

from google.colab import drive
import os

# Drive 마운트
print("📂 Google Drive 연결 중...")
drive.mount('/content/drive')

# 작업 폴더 생성
WORK_DIR = '/content/drive/MyDrive/졸업영상제작'
os.makedirs(WORK_DIR, exist_ok=True)
os.chdir(WORK_DIR)

# 하위 폴더 생성
folders = ['photos', 'videos', 'id_photos', 'baby_photos', 'data',
           'logo', 'music', 'output', 'temp']
for folder in folders:
    os.makedirs(folder, exist_ok=True)

print(f"✅ 작업 폴더 준비 완료: {WORK_DIR}")
print(f"📁 생성된 폴더: {', '.join(folders)}")
print("\n다음 단계: 파일 업로드 (셀 3-8)")
```

---

### 🔑 셀 3: API 키 설정

```python
"""
API 키 입력
- Gemini API: 필수 (무료)
- Flux API: 선택 (미래 이미지 생성용)
"""

import google.generativeai as genai
from getpass import getpass

print("🔐 API 키 설정\n")

# Gemini API 키 (필수)
print("1️⃣ Gemini API 키 입력")
print("   👉 https://makersuite.google.com/app/apikey 에서 발급")
GEMINI_API_KEY = getpass("Gemini API 키: ")

try:
    genai.configure(api_key=GEMINI_API_KEY)
    # 테스트 호출
    model = genai.GenerativeModel('gemini-2.0-flash-exp')
    test = model.generate_content("Hello")
    print("✅ Gemini API 연결 성공\n")
except Exception as e:
    print(f"❌ Gemini API 오류: {e}")
    print("키를 확인하고 다시 실행하세요.")
    raise

# Flux API 키 (선택)
print("2️⃣ Flux API 키 (선택사항)")
print("   - AI 미래 이미지 생성 기능")
print("   - 비용: 학생 1명당 약 $0.003")
print("   - 건너뛰려면 Enter만 누르세요")
FLUX_API_KEY = getpass("Flux API 키 (선택): ").strip()

if FLUX_API_KEY:
    print("✅ Flux API 사용 활성화")
    USE_FLUX = True
else:
    print("⏭️ Flux API 비활성화 (템플릿 이미지 사용)")
    USE_FLUX = False

print("\n✅ API 설정 완료")
```

---

### 📤 셀 4: 파일 업로드 유틸리티

```python
"""
파일 업로드 헬퍼 함수
- 드래그 앤 드롭 지원
- 업로드 진행률 표시
- 파일 검증
"""

from google.colab import files
import shutil
from tqdm import tqdm

def upload_files_to_folder(folder_name, file_description, file_types=None):
    """
    파일 업로드 및 검증

    Args:
        folder_name: 저장할 폴더명
        file_description: 파일 설명 (사용자 안내용)
        file_types: 허용할 파일 확장자 리스트 (예: ['.jpg', '.png'])

    Returns:
        업로드된 파일 개수
    """
    print(f"\n📤 {file_description} 업로드")
    print(f"📁 저장 위치: {folder_name}/")

    if file_types:
        print(f"✅ 허용 형식: {', '.join(file_types)}")

    print("👉 파일 선택 창에서 파일을 선택하세요...")

    try:
        uploaded = files.upload()

        if not uploaded:
            print("⚠️ 업로드된 파일이 없습니다.")
            return 0

        valid_count = 0
        invalid_files = []

        for filename, content in tqdm(uploaded.items(), desc="저장 중"):
            # 파일 확장자 검증
            if file_types:
                ext = os.path.splitext(filename)[1].lower()
                if ext not in file_types:
                    invalid_files.append(filename)
                    continue

            # 파일 저장
            filepath = os.path.join(folder_name, filename)
            with open(filepath, 'wb') as f:
                f.write(content)
            valid_count += 1

        if invalid_files:
            print(f"⚠️ 건너뛴 파일 ({len(invalid_files)}개): {', '.join(invalid_files[:5])}")

        print(f"✅ {valid_count}개 파일 업로드 완료\n")
        return valid_count

    except Exception as e:
        print(f"❌ 업로드 실패: {e}")
        return 0

print("✅ 업로드 함수 준비 완료")
```

---

### 📸 셀 5-8: 데이터 수집 (단계별 실행)

```python
# 셀 5: 사진 업로드
photo_count = upload_files_to_folder(
    'photos',
    '행사/일상 사진 (수백 장)',
    ['.jpg', '.jpeg', '.png']
)
```

```python
# 셀 6: 영상 클립 업로드
video_count = upload_files_to_folder(
    'videos',
    '영상 클립 (10개 내외)',
    ['.mp4', '.mov', '.avi']
)
```

```python
# 셀 7: 증명사진 업로드
id_photo_count = upload_files_to_folder(
    'id_photos',
    '학생 증명사진 (파일명=이름)',
    ['.jpg', '.jpeg', '.png']
)
```

```python
# 셀 8: 기타 파일 업로드
print("📤 나머지 파일 업로드\n")

# 학생 정보 엑셀
print("1️⃣ 학생 정보 엑셀 (학생정보.xlsx)")
print("   컬럼: 이름 | 어릴때사진경로 | 장래희망")
upload_files_to_folder('data', '학생정보.xlsx', ['.xlsx', '.xls'])

# 어릴 때 사진
upload_files_to_folder('baby_photos', '어릴 때 사진', ['.jpg', '.jpeg', '.png'])

# 학교 로고
upload_files_to_folder('logo', '학교 로고', ['.png', '.jpg'])

# 배경음악
upload_files_to_folder('music', '배경음악 (Suno MP3)', ['.mp3', '.wav'])

print("✅ 모든 파일 업로드 완료!")
```

---

### 📊 셀 9: 데이터 로드 및 검증

```python
"""
업로드된 파일 로드 및 검증
- 엑셀 데이터 읽기
- 파일 경로 매핑
- 데이터 무결성 검사
"""

import pandas as pd
import glob

print("📊 데이터 로드 및 검증 시작...\n")

# 1. 파일 목록 수집
photos = sorted(glob.glob('photos/*.*'))
videos = sorted(glob.glob('videos/*.*'))
id_photos = sorted(glob.glob('id_photos/*.*'))
baby_photos = sorted(glob.glob('baby_photos/*.*'))

print(f"📸 사진: {len(photos)}장")
print(f"🎬 영상: {len(videos)}개")
print(f"👤 증명사진: {len(id_photos)}장")
print(f"👶 어릴 때 사진: {len(baby_photos)}장\n")

# 2. 엑셀 파일 읽기
excel_files = glob.glob('data/*.xlsx') + glob.glob('data/*.xls')

if not excel_files:
    raise FileNotFoundError("❌ 학생 정보 엑셀 파일이 없습니다!")

excel_path = excel_files[0]
print(f"📋 엑셀 파일: {os.path.basename(excel_path)}")

try:
    student_df = pd.read_excel(excel_path)
    print(f"✅ 학생 수: {len(student_df)}명\n")

    # 필수 컬럼 검증
    required_columns = ['이름', '어릴때사진경로', '장래희망']
    missing_cols = [col for col in required_columns if col not in student_df.columns]

    if missing_cols:
        print(f"❌ 누락된 컬럼: {missing_cols}")
        print(f"   현재 컬럼: {list(student_df.columns)}")
        raise ValueError("엑셀 파일의 컬럼명을 확인하세요.")

    # 데이터 미리보기
    print("📋 학생 정보 미리보기:")
    print(student_df.head(3))

except Exception as e:
    print(f"❌ 엑셀 읽기 오류: {e}")
    raise

# 3. 로고 및 음악 파일
logo_files = glob.glob('logo/*.*')
music_files = glob.glob('music/*.*')

LOGO_PATH = logo_files[0] if logo_files else None
MUSIC_PATH = music_files[0] if music_files else None

if not LOGO_PATH:
    print("⚠️ 로고 파일 없음 (기본 타이틀만 표시)")
if not MUSIC_PATH:
    print("⚠️ 배경음악 없음 (무음 영상 생성)")

print("\n✅ 데이터 로드 완료! 시나리오 생성 준비됨")
```

---

### 🎬 셀 10: Gemini 시나리오 생성

```python
"""
Gemini AI 시나리오 생성
- 베스트 사진 30-50장 자동 선별
- 영상 클립 배치 계획
- 감동적인 흐름 설계
"""

import json
from tqdm import tqdm

def generate_graduation_scenario(photo_list, video_list, target_duration=180):
    """
    졸업영상 시나리오 자동 생성

    Args:
        photo_list: 사진 파일 경로 리스트
        video_list: 영상 파일 경로 리스트
        target_duration: 목표 길이 (초)

    Returns:
        dict: 시나리오 JSON 데이터
    """
    model = genai.GenerativeModel('gemini-2.0-flash-exp')

    # 파일명만 추출 (경로 제거)
    photo_names = [os.path.basename(p) for p in photo_list]
    video_names = [os.path.basename(v) for v in video_list]

    prompt = f"""
당신은 졸업영상 전문 감독입니다. 다음 소재로 {target_duration}초 분량의 감동적인 졸업영상을 만들어야 합니다.

📸 **사진 목록** ({len(photo_names)}장):
{', '.join(photo_names[:20])}... (이하 생략)

🎬 **영상 목록** ({len(video_names)}개):
{', '.join(video_names)}

**시나리오 작성 규칙**:
1. 사진 중 **30-50장**을 엄선하세요 (감동적이고 다양한 장면 우선)
2. 각 사진은 2-3초씩 표시
3. 영상 클립은 3-5초로 편집
4. 전체 길이: 약 {target_duration}초 (오프닝/엔딩 제외)
5. 감동적인 스토리 흐름 구성 (시작→클라이맥스→감동)

**출력 형식** (반드시 유효한 JSON):
```json
{{
  "opening": {{
    "duration": 10,
    "text": "○○초등학교 졸업을 축하합니다"
  }},
  "main_story": [
    {{
      "type": "photo",
      "file": "파일명.jpg",
      "duration": 2.5,
      "description": "입학식 첫날"
    }},
    {{
      "type": "video",
      "file": "파일명.mp4",
      "duration": 4,
      "description": "운동회 단체 줄넘기"
    }}
  ],
  "total_duration": 180
}}
```

**중요**: 반드시 유효한 JSON 형식으로만 출력하세요. 주석이나 설명 없이 JSON만 출력하세요.
"""

    print("🤖 Gemini AI 시나리오 생성 중...")

    try:
        response = model.generate_content(prompt)
        response_text = response.text.strip()

        # JSON 추출 (코드 블록 제거)
        if '```json' in response_text:
            response_text = response_text.split('```json')[1].split('```')[0].strip()
        elif '```' in response_text:
            response_text = response_text.split('```')[1].split('```')[0].strip()

        # JSON 파싱
        scenario = json.loads(response_text)

        print(f"✅ 시나리오 생성 완료!")
        print(f"   - 선별된 사진: {sum(1 for s in scenario['main_story'] if s['type'] == 'photo')}장")
        print(f"   - 영상 클립: {sum(1 for s in scenario['main_story'] if s['type'] == 'video')}개")
        print(f"   - 예상 길이: {scenario.get('total_duration', 0)}초")

        return scenario

    except json.JSONDecodeError as e:
        print(f"❌ JSON 파싱 실패: {e}")
        print(f"응답 내용:\n{response_text[:500]}")
        raise
    except Exception as e:
        print(f"❌ 시나리오 생성 실패: {e}")
        raise

# 시나리오 생성 실행
scenario = generate_graduation_scenario(photos, videos, target_duration=180)

# 시나리오 저장 (백업)
with open('temp/scenario.json', 'w', encoding='utf-8') as f:
    json.dump(scenario, f, ensure_ascii=False, indent=2)

print("\n💾 시나리오 저장: temp/scenario.json")
```

---

### 🎵 셀 11: Suno 가사 생성 (선택)

```python
"""
Suno AI용 졸업 노래 가사 생성
- 사용자가 직접 Suno에서 음악 제작 후 업로드
"""

def generate_suno_lyrics():
    """감동적인 졸업 노래 가사 생성"""
    model = genai.GenerativeModel('gemini-2.0-flash-exp')

    prompt = """
초등학교 졸업식 뮤직비디오용 노래 가사를 작성하세요.

**요구사항**:
- 길이: 2-3분 (verse-chorus-verse-chorus-bridge-chorus)
- 테마: 우정, 추억, 도전, 희망, 감사
- 톤: 감동적이면서도 밝고 희망적
- Suno AI에 바로 복사 가능한 형식

**출력 형식**:
[Verse 1]
가사 내용...

[Chorus]
가사 내용...

[Verse 2]
...

[Bridge]
...

[Chorus]
...
"""

    response = model.generate_content(prompt)
    return response.text

print("🎵 Suno AI 가사 생성 중...\n")
lyrics = generate_suno_lyrics()

print("=" * 50)
print(lyrics)
print("=" * 50)

print("\n📋 다음 단계:")
print("1. 위 가사를 복사하세요")
print("2. https://suno.ai 접속")
print("3. 가사 붙여넣기 → 음악 스타일 선택 (예: Pop, Ballad)")
print("4. 생성된 MP3 다운로드")
print("5. 셀 8에서 배경음악으로 업로드")

# 가사 저장
with open('temp/suno_lyrics.txt', 'w', encoding='utf-8') as f:
    f.write(lyrics)

print("\n💾 가사 저장: temp/suno_lyrics.txt")
```

---

### 🎞️ 셀 12: Module 1 - 메인 스토리 생성

```python
"""
메인 스토리 영상 생성
- 오프닝
- 시나리오 기반 사진/영상 편집
- Ken Burns 효과
"""

from moviepy.editor import *
from tqdm import tqdm
import random

def apply_ken_burns(image_path, duration):
    """
    Ken Burns 효과 (줌인/패닝)

    Args:
        image_path: 이미지 파일 경로
        duration: 지속 시간 (초)

    Returns:
        VideoClip
    """
    try:
        clip = ImageClip(image_path, duration=duration)

        # 이미지 크기
        w, h = clip.size

        # 랜덤 효과 선택
        effect = random.choice(['zoom_in', 'zoom_out', 'pan_right', 'pan_left'])

        if effect == 'zoom_in':
            clip = clip.resize(lambda t: 1 + 0.1 * t / duration)
        elif effect == 'zoom_out':
            clip = clip.resize(lambda t: 1.1 - 0.1 * t / duration)
        elif effect == 'pan_right':
            clip = clip.set_position(lambda t: (- w * 0.1 * t / duration, 'center'))
        elif effect == 'pan_left':
            clip = clip.set_position(lambda t: (w * 0.1 * t / duration, 'center'))

        return clip.resize(height=1080).set_position('center')

    except Exception as e:
        print(f"⚠️ Ken Burns 효과 실패 ({os.path.basename(image_path)}): {e}")
        # 기본 클립 반환
        return ImageClip(image_path, duration=duration).resize(height=1080).set_position('center')


def create_main_story(scenario, photo_folder, video_folder, logo_path, school_name="○○초등학교"):
    """
    메인 스토리 영상 생성

    Returns:
        VideoClip
    """
    clips = []

    print("🎬 메인 스토리 생성 중...\n")

    # 1. 오프닝
    print("1️⃣ 오프닝 제작...")
    try:
        if logo_path and os.path.exists(logo_path):
            logo = ImageClip(logo_path, duration=3) \
                .resize(height=600) \
                .set_position("center") \
                .fadein(1).fadeout(1)

            title = TextClip(f"{school_name}\n졸업을 축하합니다",
                           fontsize=80, color='white', font='NanumGothic',
                           size=(1920, 1080), method='caption') \
                .set_duration(3).set_position("center")

            opening = CompositeVideoClip([
                ColorClip(size=(1920, 1080), color=(0, 0, 0), duration=3),
                logo,
                title
            ])
        else:
            # 로고 없을 경우
            opening = TextClip(f"{school_name}\n졸업을 축하합니다",
                             fontsize=100, color='white', font='NanumGothic',
                             bg_color='black',
                             size=(1920, 1080), method='caption') \
                .set_duration(5).fadein(1).fadeout(1)

        clips.append(opening)
        print("   ✅ 오프닝 완료\n")

    except Exception as e:
        print(f"   ⚠️ 오프닝 생성 실패: {e}\n")

    # 2. 메인 장면들
    print("2️⃣ 메인 장면 생성...")

    for idx, scene in enumerate(tqdm(scenario['main_story'], desc="장면 처리")):
        try:
            if scene['type'] == 'photo':
                # 사진 클립
                photo_path = os.path.join(photo_folder, scene['file'])

                if not os.path.exists(photo_path):
                    print(f"   ⚠️ 파일 없음: {scene['file']}")
                    continue

                clip = apply_ken_burns(photo_path, scene['duration']) \
                    .crossfadein(0.5)

            elif scene['type'] == 'video':
                # 영상 클립
                video_path = os.path.join(video_folder, scene['file'])

                if not os.path.exists(video_path):
                    print(f"   ⚠️ 파일 없음: {scene['file']}")
                    continue

                original_clip = VideoFileClip(video_path)

                # 길이 조정
                if original_clip.duration > scene['duration']:
                    clip = original_clip.subclip(0, scene['duration'])
                else:
                    clip = original_clip

                clip = clip.resize(height=1080).set_position('center').crossfadein(0.5)

            clips.append(clip)

        except Exception as e:
            print(f"   ⚠️ 장면 {idx+1} 처리 실패: {e}")
            continue

    print(f"   ✅ {len(clips)-1}개 장면 생성 완료\n")

    # 3. 클립 연결
    print("3️⃣ 장면 연결 중...")
    main_video = concatenate_videoclips(clips, method="compose")
    print(f"   ✅ 메인 스토리 완료 (길이: {main_video.duration:.1f}초)\n")

    return main_video

# 메인 스토리 생성 실행
main_story_clip = create_main_story(scenario, 'photos', 'videos', LOGO_PATH)

print("💾 임시 저장 중...")
main_story_clip.write_videofile(
    'temp/main_story.mp4',
    fps=30,
    codec='libx264',
    audio=False,
    preset='ultrafast',
    threads=4
)
print("✅ 메인 스토리 임시 저장 완료")
```

---

### 👥 셀 13: Module 2 - 올스타 롤콜

```python
"""
증명사진 롤콜
- 학생당 0.5초 빠른 슬라이드
- 이름 자막
"""

from moviepy.editor import *
from tqdm import tqdm

def create_roll_call(id_photo_folder, student_df):
    """
    올스타 롤콜 생성

    Returns:
        VideoClip
    """
    clips = []

    print("👥 올스타 롤콜 생성 중...\n")

    # 증명사진 파일 매핑
    id_photo_files = {
        os.path.splitext(os.path.basename(f))[0]: f
        for f in glob.glob(f'{id_photo_folder}/*.*')
    }

    for idx, row in tqdm(student_df.iterrows(), total=len(student_df), desc="학생 처리"):
        try:
            student_name = row['이름']

            # 파일 찾기
            photo_file = id_photo_files.get(student_name)

            if not photo_file:
                print(f"⚠️ {student_name} 증명사진 없음")
                continue

            # 사진 클립
            photo_clip = ImageClip(photo_file, duration=0.5) \
                .resize(height=800) \
                .set_position("center")

            # 이름 자막
            name_text = TextClip(student_name,
                               fontsize=60,
                               color='white',
                               font='NanumGothic',
                               stroke_color='black',
                               stroke_width=2) \
                .set_duration(0.5) \
                .set_position(("center", 950))

            # 배경
            bg = ColorClip(size=(1920, 1080), color=(30, 30, 30), duration=0.5)

            # 합성
            composite = CompositeVideoClip([bg, photo_clip, name_text])
            clips.append(composite)

        except Exception as e:
            print(f"⚠️ {student_name} 처리 실패: {e}")
            continue

    print(f"\n✅ {len(clips)}명 롤콜 완료\n")

    if not clips:
        print("⚠️ 롤콜 클립 없음 - 건너뜀")
        return None

    # 타이틀 추가
    title = TextClip("🌟 올스타 롤콜 🌟",
                    fontsize=100,
                    color='yellow',
                    font='NanumGothic',
                    bg_color='black',
                    size=(1920, 1080),
                    method='caption') \
        .set_duration(2).fadein(0.5).fadeout(0.5)

    roll_call_video = concatenate_videoclips([title] + clips)

    print(f"✅ 롤콜 영상 완료 (길이: {roll_call_video.duration:.1f}초)")
    return roll_call_video

# 롤콜 생성 실행
roll_call_clip = create_roll_call('id_photos', student_df)

if roll_call_clip:
    print("💾 임시 저장 중...")
    roll_call_clip.write_videofile(
        'temp/roll_call.mp4',
        fps=30,
        codec='libx264',
        audio=False,
        preset='ultrafast',
        threads=4
    )
```

---

### ⏰ 셀 14: Module 3 - 시간여행 (선택적 AI 이미지 생성)

```python
"""
시간여행: 과거 → 현재 → 미래
- AI 미래 이미지 (선택)
- 장래희망 자막
"""

from moviepy.editor import *
from PIL import Image, ImageDraw, ImageFont
import requests
from tqdm import tqdm

def create_future_image_template(dream_job, output_path):
    """
    Flux API 없이 템플릿 이미지 생성

    Args:
        dream_job: 장래희망
        output_path: 저장 경로
    """
    try:
        # 1920x1080 빈 이미지
        img = Image.new('RGB', (1920, 1080), color=(50, 50, 80))
        draw = ImageDraw.Draw(img)

        # 텍스트 추가
        try:
            font = ImageFont.truetype("/usr/share/fonts/truetype/nanum/NanumGothic.ttf", 120)
        except:
            font = ImageFont.load_default()

        text = f"미래의 {dream_job}"

        # 텍스트 크기 계산 (PIL 10+ 호환)
        bbox = draw.textbbox((0, 0), text, font=font)
        text_width = bbox[2] - bbox[0]
        text_height = bbox[3] - bbox[1]

        position = ((1920 - text_width) // 2, (1080 - text_height) // 2)

        draw.text(position, text, fill=(255, 255, 255), font=font)

        img.save(output_path)
        return output_path

    except Exception as e:
        print(f"⚠️ 템플릿 생성 실패: {e}")
        return None


def generate_future_image_flux(current_photo_path, dream_job, api_key):
    """
    Flux API로 미래 이미지 생성 (유료)

    Returns:
        str: 생성된 이미지 경로
    """
    try:
        import replicate

        prompt = f"A professional {dream_job} in their 30s, realistic portrait, Korean person, professional attire, warm lighting"

        output = replicate.run(
            "black-forest-labs/flux-schnell",
            input={
                "prompt": prompt,
                "num_outputs": 1,
                "aspect_ratio": "16:9"
            }
        )

        # 이미지 다운로드
        image_url = output[0]
        response = requests.get(image_url)

        output_path = f"temp/future_{dream_job}.jpg"
        with open(output_path, 'wb') as f:
            f.write(response.content)

        return output_path

    except Exception as e:
        print(f"⚠️ Flux API 실패: {e}")
        return None


def create_time_travel(student_df, baby_folder, id_photo_folder, use_flux=False, flux_api_key=None):
    """
    시간여행 영상 생성

    Returns:
        VideoClip or None
    """
    clips = []

    print("⏰ 시간여행 생성 중...\n")

    # 증명사진 매핑
    id_photo_files = {
        os.path.splitext(os.path.basename(f))[0]: f
        for f in glob.glob(f'{id_photo_folder}/*.*')
    }

    for idx, row in tqdm(student_df.iterrows(), total=len(student_df), desc="학생 처리"):
        try:
            student_name = row['이름']
            baby_photo_path = row['어릴때사진경로']
            dream_job = row['장래희망']

            # 1. 어릴 때 사진
            if not os.path.exists(baby_photo_path):
                print(f"⚠️ {student_name} 어릴 때 사진 없음")
                continue

            baby_clip = ImageClip(baby_photo_path, duration=1) \
                .resize(height=1080).set_position('center')

            # 2. 현재 사진
            current_photo_path = id_photo_files.get(student_name)
            if not current_photo_path:
                print(f"⚠️ {student_name} 현재 사진 없음")
                continue

            current_clip = ImageClip(current_photo_path, duration=1) \
                .resize(height=1080).set_position('center')

            # 3. 미래 이미지
            if use_flux and flux_api_key:
                future_path = generate_future_image_flux(current_photo_path, dream_job, flux_api_key)
            else:
                future_path = create_future_image_template(dream_job, f'temp/future_{student_name}.jpg')

            if not future_path:
                print(f"⚠️ {student_name} 미래 이미지 생성 실패")
                future_path = current_photo_path  # 현재 사진으로 대체

            future_clip = ImageClip(future_path, duration=1) \
                .resize(height=1080).set_position('center')

            # 4. 3장 연결
            sequence = concatenate_videoclips([baby_clip, current_clip, future_clip])

            # 5. 자막 추가
            dream_text = TextClip(f"{student_name} - {dream_job}",
                                fontsize=70,
                                color='yellow',
                                font='NanumGothic',
                                stroke_color='black',
                                stroke_width=3) \
                .set_duration(3) \
                .set_position(("center", 950))

            composite = CompositeVideoClip([sequence, dream_text])
            clips.append(composite)

        except Exception as e:
            print(f"⚠️ {student_name} 시간여행 실패: {e}")
            continue

    if not clips:
        print("⚠️ 시간여행 클립 없음 - 건너뜀")
        return None

    print(f"\n✅ {len(clips)}명 시간여행 완료\n")

    # 타이틀 추가
    title = TextClip("⏰ 시간여행: 과거·현재·미래 ⏰",
                    fontsize=90,
                    color='cyan',
                    font='NanumGothic',
                    bg_color='black',
                    size=(1920, 1080),
                    method='caption') \
        .set_duration(2).fadein(0.5).fadeout(0.5)

    time_travel_video = concatenate_videoclips([title] + clips)

    print(f"✅ 시간여행 영상 완료 (길이: {time_travel_video.duration:.1f}초)")
    return time_travel_video

# 시간여행 생성 실행
time_travel_clip = create_time_travel(
    student_df,
    'baby_photos',
    'id_photos',
    use_flux=USE_FLUX,
    flux_api_key=FLUX_API_KEY if USE_FLUX else None
)

if time_travel_clip:
    print("💾 임시 저장 중...")
    time_travel_clip.write_videofile(
        'temp/time_travel.mp4',
        fps=30,
        codec='libx264',
        audio=False,
        preset='ultrafast',
        threads=4
    )
```

---

### 🎉 셀 15: 최종 조립 및 렌더링

```python
"""
최종 영상 조립
- 모든 모듈 결합
- 배경음악 추가
- 크레딧 롤
- Full HD 렌더링
"""

from moviepy.editor import *
from datetime import datetime

def create_ending_credits(student_df, duration=15):
    """
    엔딩 크레딧 생성

    Returns:
        VideoClip
    """
    # 졸업생 명단
    names = student_df['이름'].tolist()

    # 3열로 배치
    rows = []
    for i in range(0, len(names), 3):
        row_names = names[i:i+3]
        rows.append("    ".join(row_names))

    credits_text = "🎓 졸업생 명단 🎓\n\n" + "\n".join(rows)

    credits = TextClip(credits_text,
                      fontsize=45,
                      color='white',
                      font='NanumGothic',
                      bg_color='black',
                      method='caption',
                      size=(1920, 1080)) \
        .set_duration(duration) \
        .fadein(1).fadeout(1)

    return credits


def final_assembly(modules, music_path, student_df, school_name="○○초등학교"):
    """
    최종 영상 조립

    Args:
        modules: dict - {'main': clip, 'roll_call': clip, 'time_travel': clip}
        music_path: 배경음악 경로
        student_df: 학생 데이터프레임

    Returns:
        str: 출력 파일 경로
    """
    print("🎬 최종 조립 시작...\n")

    # 1. 모듈 결합
    video_clips = []

    if modules.get('main'):
        video_clips.append(modules['main'])
        print(f"✅ 메인 스토리: {modules['main'].duration:.1f}초")

    if modules.get('roll_call'):
        video_clips.append(modules['roll_call'])
        print(f"✅ 올스타 롤콜: {modules['roll_call'].duration:.1f}초")

    if modules.get('time_travel'):
        video_clips.append(modules['time_travel'])
        print(f"✅ 시간여행: {modules['time_travel'].duration:.1f}초")

    if not video_clips:
        raise ValueError("❌ 조립할 클립이 없습니다!")

    # 엔딩 크레딧
    credits = create_ending_credits(student_df, duration=15)
    video_clips.append(credits)
    print(f"✅ 엔딩 크레딧: {credits.duration:.1f}초\n")

    # 비디오 결합
    print("🔗 클립 결합 중...")
    final_video = concatenate_videoclips(video_clips, method="compose")
    total_duration = final_video.duration
    print(f"✅ 총 영상 길이: {total_duration:.1f}초 ({total_duration/60:.1f}분)\n")

    # 2. 배경음악 추가
    if music_path and os.path.exists(music_path):
        print("🎵 배경음악 추가 중...")

        try:
            audio = AudioFileClip(music_path)

            # 음악 길이 조정
            if audio.duration > total_duration:
                audio = audio.subclip(0, total_duration).audio_fadeout(3)
            else:
                # 음악이 짧으면 반복
                loops = int(total_duration / audio.duration) + 1
                audio = concatenate_audioclips([audio] * loops).subclip(0, total_duration)

            final_video = final_video.set_audio(audio)
            print("✅ 배경음악 추가 완료\n")

        except Exception as e:
            print(f"⚠️ 배경음악 추가 실패: {e}")
            print("   무음 영상으로 진행합니다.\n")
    else:
        print("⚠️ 배경음악 없음 - 무음 영상\n")

    # 3. 렌더링
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_path = f"output/{school_name}_졸업영상_{timestamp}.mp4"

    print("🎬 최종 렌더링 시작...")
    print(f"   출력: {output_path}")
    print(f"   해상도: 1920x1080 (Full HD)")
    print(f"   코덱: H.264 (최대 호환성)")
    print(f"   예상 시간: {total_duration/60*2:.0f}분 (실시간의 2배)\n")

    final_video.write_videofile(
        output_path,
        fps=30,
        codec='libx264',
        audio_codec='aac',
        bitrate='8000k',  # 고품질
        preset='medium',
        threads=4,
        logger='bar'  # 진행률 표시
    )

    # 파일 크기 확인
    file_size_mb = os.path.getsize(output_path) / (1024 * 1024)

    print("\n" + "="*60)
    print("🎉 렌더링 완료!")
    print("="*60)
    print(f"📁 파일: {output_path}")
    print(f"📊 크기: {file_size_mb:.1f} MB")
    print(f"⏱️ 길이: {total_duration/60:.1f}분")
    print(f"🎬 해상도: 1920x1080 @ 30fps")
    print("="*60)

    return output_path

# 최종 조립 실행
print("=" * 60)
print("🚀 최종 영상 제작 시작")
print("=" * 60 + "\n")

modules = {}

# 메인 스토리 로드
if os.path.exists('temp/main_story.mp4'):
    modules['main'] = VideoFileClip('temp/main_story.mp4')

# 롤콜 로드
if os.path.exists('temp/roll_call.mp4'):
    modules['roll_call'] = VideoFileClip('temp/roll_call.mp4')

# 시간여행 로드
if os.path.exists('temp/time_travel.mp4'):
    modules['time_travel'] = VideoFileClip('temp/time_travel.mp4')

# 최종 조립
output_file = final_assembly(
    modules,
    MUSIC_PATH,
    student_df,
    school_name="우리초등학교"  # 여기 수정
)

print(f"\n✅ 최종 파일: {output_file}")
```

---

### 💾 셀 16: 다운로드

```python
"""
완성된 영상 다운로드
"""

from google.colab import files
import glob

print("📥 영상 다운로드\n")

# 최신 출력 파일 찾기
output_files = sorted(glob.glob('output/*.mp4'), key=os.path.getmtime, reverse=True)

if output_files:
    latest_file = output_files[0]

    print(f"📁 파일: {os.path.basename(latest_file)}")
    print(f"📊 크기: {os.path.getsize(latest_file) / (1024*1024):.1f} MB")
    print("\n⏬ 다운로드 시작...\n")

    files.download(latest_file)

    print("\n✅ 다운로드 완료!")
    print("\n📺 재생 확인 사항:")
    print("   - VLC, Windows Media Player 등으로 재생 테스트")
    print("   - 프로젝터에서 Full HD(1920x1080) 출력 확인")
    print("   - 음량 레벨 체크")
else:
    print("❌ 출력 파일을 찾을 수 없습니다.")
    print("   셀 15(최종 조립)를 먼저 실행하세요.")
```

---

## 📋 사용 가이드

### 🚀 빠른 시작

1. **Google Colab 노트북 생성**
   - https://colab.research.google.com 접속
   - 새 노트북 생성
   - 위 셀들을 순서대로 복사

2. **셀 실행 순서**
   ```
   셀 1 → 셀 2 → 셀 3 → 셀 4 → 셀 5-8 (파일 업로드)
   → 셀 9 (검증) → 셀 10 (시나리오)
   → 셀 12 (메인) → 셀 13 (롤콜) → 셀 14 (시간여행)
   → 셀 15 (조립) → 셀 16 (다운로드)
   ```

3. **예상 소요 시간**
   - 환경 설정: 5분
   - 파일 업로드: 10-20분 (용량에 따라)
   - 시나리오 생성: 1-2분
   - 영상 제작: 20-40분
   - **총: 약 1-1.5시간**

---

## ⚠️ 주의사항

### Colab 무료 버전 제한
- **세션 시간**: 최대 12시간 (유휴 90분 시 종료)
- **해결책**: Google Drive 마운트로 자동 저장
- **GPU**: 불필요 (CPU만 사용)

### 비용 관련
- **Gemini API**: 무료 (월 60회 제한)
- **Flux API**: 선택적 사용 ($0.003/이미지)
- **총 예상 비용**: $0 ~ $0.1 (학생 30명 기준)

### 파일 용량
- **최종 영상**: 약 300-500 MB (5분 기준)
- **작업 공간**: 최소 2 GB 여유 필요

---

## 🐛 트러블슈팅

### 자주 발생하는 오류

#### 1. "ModuleNotFoundError: No module named 'moviepy'"
```python
# 셀 1을 다시 실행하세요
!pip install moviepy --upgrade
```

#### 2. "Font 'NanumGothic' not found"
```python
# 셀 1의 폰트 설치 부분 재실행
!apt-get install -y fonts-nanum
!fc-cache -fv
```

#### 3. "Session timeout"
- Google Drive에 중간 결과가 저장되어 있으므로
- 셀 15(최종 조립)부터 다시 실행 가능

#### 4. "Memory error"
- 사진 해상도 낮추기 (4K → Full HD)
- 영상 클립 개수 줄이기

---

## 📞 지원

**개발자**: Claude AI
**버전**: 2.0 (2024)
**라이선스**: MIT

**피드백**: 이 문서에 대한 개선 제안은 환영합니다!

---

## 🎓 마무리

이 시스템으로 **수작업 대비 90% 시간 절감** 가능합니다.

- 기존: 3-5일 (전문가 작업)
- 자동화: 1.5시간 (비전문가도 가능)

**감동적인 졸업영상을 만들어 평생의 추억을 선물하세요!** 🎉
